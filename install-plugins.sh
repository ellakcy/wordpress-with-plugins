#!/bin/bash
# COMMAND='wp --allow-root --path=/var/www/html'

#Append below for the plugin installation
echo "Here"
# ${COMMAND} plugin install plugin piwik
# ${COMMAND} plugin install plugin twitter
